import 'package:flutter/material.dart';
import 'event.dart';

class OpportunityForm extends StatelessWidget {
  const OpportunityForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const PostForm(type: 'Opportunity');
  }
}
